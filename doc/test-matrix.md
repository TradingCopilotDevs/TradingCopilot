# 测试矩阵

- 2026-06-13：完成消息触发 prediction meeting 后的 evidence chain 可见性与 prediction watchlist/会议结论互跳：会议 trust 面板新增结构化证据列表，`prediction_watchlist_updated` 可展示工具、事件、market/event 身份并跳到预测市场页；`prediction_watchlist_items` 新增 `sourceMeetingId`、`sourceMeetingEventId`、`sourceRoleKey`，会议 recap 写入时保存来源，关注列表可跳回会议，手工更新不清空来源，删除会议时清理坏链接。预测市场搜索页新增 URL/slug 归一化提示、provider 失败但本地缓存命中的 warning，以及 provider 失败且无缓存命中的 error/empty-state 说明。已通过 `go test ./internal/app/prediction -run TestUpsertWatchlistRequiresPredictionOrMixedTeamAndExistingMarket -count=1 -v`、`go test ./internal/infra/persistence/gorm/repo -run "TestPredictionRepository(UpsertWatchlistKeepsSourceWhenManualUpdateHasNoSource|SearchMarketsCoversEventAndIdentifiers)|TestMeetingRepositoryDeleteGraphClearsPredictionWatchlistSource" -count=1 -v`、`go test ./internal/infra/persistence/gorm/meeting -run TestApplyMeetingRecapActionsAppliesPredictionWatchlistActions -count=1 -v`、`go generate ./internal/transport/http/openapi`、`npm run typegen:api --prefix frontend` 和 `npm run build --prefix frontend`。
- 2026-06-13：补强 prediction watchlist 更新的会议证据可见性：`prediction_watchlist_updated` 事件现在带 `tool=prediction.upsert_watchlist`、本地 `market_id`、market question/slug 和 event 身份，并被 trust report 计入结构化 evidence；会议详情可追溯关注动作实际写入了哪个 Polymarket market，而不是只在预测市场页看到结果。已通过 `go test ./internal/infra/persistence/gorm/meeting -run TestApplyMeetingRecapActionsAppliesPredictionWatchlistActions -count=1 -v`、`go test ./internal/transport/http -run TestBuildMeetingTrustReportCountsPredictionWatchlistUpdatesAsEvidence -count=1 -v`。
- 2026-06-13：优化 prediction recap action 的人工复核展示：trust report 保留 `prediction_watchlist` action type，会议详情页将其显示为“预测关注”，并展示本地 `market_id`、关注/停用状态和 note/reason；A 股 `watchlist` 仍显示证券自选字段，避免人工复核时把预测市场关注误读成 A 股自选或模拟盘动作。已通过 `go test ./internal/transport/http -run TestBuildMeetingTrustReportKeepsPredictionWatchlistActionType -count=1 -v` 和 `npm run build --prefix frontend`。
- 2026-06-13：收紧 prediction meeting 的历史引用/工具边界：预测会议不再按 `meeting.*` 前缀放行未来通用会议工具，只允许 `meeting.references` 和 `meeting.transcript`；`meeting.references` 在 prediction meeting 中会保留 prediction-market 引用和带 prediction context 的历史会议引用，但会 redacted 非 prediction 历史会议的 note/topic/summary 快照，避免手动链接 A 股会议摘要进入角色上下文或 moderator recap。已通过 `go test ./internal/app/research ./internal/infra/persistence/gorm/meeting -run "TestUpsertRoleSanitizesPredictionTeamCapabilities|TestPredictionMeetingReferencesRedactNonPredictionSnapshots|TestPredictionMeetingBlocksUnknownMeetingNamespaceTools" -count=1 -v`。
- 2026-06-13：补齐预测市场关注读取工具：新增只读 `prediction.watchlist` 会议工具，返回当前 prediction research team 的关注项、note/active 和 market/event 身份快照；默认只分配给主持人和结论整理员，用于在 `prediction_watchlist_actions` 前判断新增/更新/重新启用/停用，避免重复关注或覆盖已有跟踪理由。已通过 `go test ./internal/app/ai ./internal/app/research ./internal/infra/persistence/gorm/meeting -run "TestPredictionToolDefinitionsExplainOutcomeTokenMapping|TestPredictionMarketDefaultRolesUseOnlyResearchEvidenceTools|TestPredictionWatchlistToolReturnsTeamScopedMarketSnapshots" -count=1 -v`。
- 2026-06-13：使用 live Gamma payload 复核 Polymarket URL 搜索：示例 event endpoint `events/slug/us-x-iran-permanent-peace-deal-by` 当前返回 event `357807` 和 17 个子市场；同时修复 direct market slug 返回中 `eventId=null`、父 event 仅位于 `events` 数组的问题，解析器会持久化 embedded event 并把 market 关联到正确本地 event。已通过 `go test ./internal/infra/prediction ./internal/app/prediction -run "TestSearchUsesEventSlugEndpointForPolymarketURL|TestSearchFallsBackToMarketSlugEndpoint|TestSearchPersistsMarketSlugEmbeddedEventIdentity" -count=1 -v`。
- 2026-06-13：补齐预测市场关注动作闭环：新增 `prediction.upsert_watchlist` 作为讨论阶段延迟动作工具，moderator recap 使用 `prediction_watchlist_actions` 按本地 `market_id` 写入预测市场关注列表；A 股 `watchlist_actions` 在预测会议中仍被阻断，预测关注动作同样需要 facts/inferences/citations 证据约束。已通过 `go test ./internal/app/meeting ./internal/app/research ./internal/infra/persistence/gorm/meeting -run "TestValidateModeratorRecapActionsRequiresEvidenceForExecutableActions|TestPredictionMarketDefaultRolesUseOnlyResearchEvidenceTools|TestPredictionMarketDefaultRolePromptsCoverResolutionOddsRiskAndActionBoundary|TestApplyMeetingRecapActionsAppliesPredictionWatchlistActions|TestModeratorRecapPromptForPredictionMarketForbidsExecutableActions" -count=1 -v`。
- 2026-06-13：收紧预测市场 query/匹配证据的 A 股噪声过滤：`BuildQuery` 和 `ScoreMatch` 共用 term 过滤，剔除 A 股 6 位代码、A 股/证券/模拟盘/仓位/买卖等交易噪声；在 A 股语境下额外忽略 `paper`、`order`、`position`、`share/stock/security` 等英文伪证据，避免旧 A 股摘要把错误 overlap 带入 Polymarket 搜索和自动关联。已通过 `go test ./internal/app/prediction -run "TestBuildQueryDropsAShareAndTradingArtifacts|TestScoreMatchIgnoresAShareNoiseTerms" -count=1 -v`。
- 2026-06-13：优化预测市场本地缓存兜底排序：provider 不可用时，`SearchMarkets` 会先返回 exact market slug/external id/condition id，再返回 exact event slug/external event id，之后才按部分匹配、活跃/关闭状态和 volume 排序，避免事件 URL 已缓存的子市场被高成交量部分匹配结果挤到前面。已通过 `go test ./internal/infra/persistence/gorm/repo -run "TestPredictionRepositorySearchMarketsCoversEventAndIdentifiers|TestPredictionRepositorySearchMarketsRanksExactEventSlugBeforePartialHighVolumeMarket" -count=1 -v`。
- 2026-06-13：新增托管 prediction meeting 角色发言边界门禁：角色 `analysis` 和 `tool_request` 在进入讨论上下文前会拒绝 A 股、证券代码、模拟盘、买卖/仓位、`market.*`/`paper.*` 等污染内容；模型先收到重写提示，连续越界时只写入 `prediction_market_role_analysis_blocked` 的干净占位发言，避免污染后续主持人 recap。已通过 `go test ./internal/infra/persistence/gorm/meeting -run "TestPredictionMarketRoleTurnValidationRejectsAShareAnalysisAndToolRequests|TestRunMeetingOnceManagedPredictionRoleBlocksAShareAnalysisBeforeRecap|TestPredictionMarketRecapValidationRejectsAShareText|TestManagedPromptsForPredictionMarketAvoidAShareTradingFrame" -count=1 -v` 和 `go test ./internal/infra/persistence/gorm/meeting -run "TestRunMeetingOnceManagedRunnerPlansToolRequestsAndRecap|TestRunMeetingOnceManagedRunnerRetriesInvalidWakePlanRecap|TestRunMeetingOnceManagedRunnerRetriesActionRecapWithoutEvidence" -count=1 -v`。
- 2026-06-13：继续打磨预测市场 URL 搜索体验：Polymarket `/event/`、`/events/`、`/market/`、`/markets/` 路径、无协议裸域名、分享 fragment 和大小写混用 slug 都会归一为小写 Gamma slug；会议工具 `prediction.search_markets` 在保留可串联本地 market id 的同时记录 `original_query` 与 `normalized_query`，便于审计角色实际搜索内容。已通过 `go test ./internal/app/prediction -run "TestNormalizeSearchQueryExtractsPolymarketSlug|TestSearchNormalizesPolymarketURLForProviderAndRepository|TestSearchReturnsCachedRowsWithProviderWarning" -count=1 -v`、`go test ./internal/infra/prediction -run "TestSearchUsesEventSlugEndpointForPolymarketURL|TestSearchFallsBackToMarketSlugEndpoint|TestSearchRequestsFullPublicSearchPayload" -count=1 -v` 和 `go test ./internal/infra/persistence/gorm/meeting -run "TestPredictionSearchToolPersistsAndReturnsLocalMarketID|TestPredictionToolsExposeOutcomeTokenMapping" -count=1 -v`。
- 2026-06-13：补强预测市场会议工具的 event/market 身份链路：`prediction.search_markets`、`prediction.market_snapshot`、`prediction.orderbook`、`prediction.price_history` 和 `prediction.related_matches` 的 market 明细除本地 `id` 外会输出本地 `event_id`、`external_event_id`、`event_slug` 和 `event_title`，便于角色审计 Polymarket event URL 命中的子 market 是否属于同一事件，并继续串联 snapshot/orderbook/price history。已通过 `go test ./internal/infra/persistence/gorm/meeting -run "TestPredictionSearchToolPersistsAndReturnsLocalMarketID|TestPredictionToolsExposeOutcomeTokenMapping" -count=1 -v`。
- 2026-06-13：补强预测市场 HTTP/前端 event 身份展示：`PredictionMarketAttributes`、搜索结果、市场详情、match/watchlist 嵌套 market 会输出 `externalEventId`、`eventSlug`、`eventTitle`；前端预测市场页在市场发现、关注列表和匹配治理表展示可读 event 归属，避免只显示本地 event 数字 ID。已通过 `go test ./internal/infra/persistence/gorm/repo -run "TestPredictionRepositorySearchMarketsCoversEventAndIdentifiers|TestPredictionRepositorySearchMarketsRanksExactEventSlugBeforePartialHighVolumeMarket" -count=1 -v`、`go test ./internal/architecture -run TestPredictionMarketContractIsExplicit -count=1`、`go generate ./internal/transport/http/openapi`、`npm run typegen:api --prefix frontend` 和 `npm run build --prefix frontend`。
- 2026-06-13：收紧预测市场新闻自动匹配证据与自动关联门槛：`scoreBreakdown` 输出 overlap terms、term 计数和 event 身份字段，`candidateSnapshot` 输出稳定 snake_case market/event 身份；`linked` 自动关联要求分数达标且至少 2 个不同关键词重合，单词重合只进入人工复核。已通过 `go test ./internal/app/prediction -run "TestScoreMatch|TestMatchNews|TestBuildQuery|TestSearch" -count=1 -v`。
- 2026-06-13：收紧预测市场 HTTP 契约：OpenAPI 不再把 `prediction-market-matches` 和 `prediction-watchlist-items` 作为弱 `additionalProperties` 资源，显式声明 match/watchlist attributes、嵌套 market 快照、sync/event/market 详情响应，并修正 `PredictionMarketAttributes` 与服务端返回字段一致；前端预测市场页改为使用生成 schema 派生类型。已通过 `go generate ./internal/transport/http/openapi`、`npm run typegen:api --prefix frontend`、`go test ./internal/architecture -run TestPredictionMarketContractIsExplicit -count=1`、`go test ./internal/transport/http -count=1` 和 `npm run build --prefix frontend`。
- 2026-06-13：新增预测市场 recap 文本边界门禁：托管 prediction meeting 的最终 recap 若在 topic/tags/summary/conclusion/facts/assumptions/inferences/evidence_gaps/citations 中混入 A 股、股票/证券、6 位 A 股/ETF 代码、模拟盘、买卖/仓位/下单或 A 股行情/模拟盘工具引用，会触发模型重写；旧式 moderator recap 事件会写入 `prediction_market_recap_text_blocked` 并拒绝应用为最终结论。动作 spec 与 wake plan spec 仍走各自专门门禁，避免误伤合法观察型 time/event 唤醒。已通过 `go test ./internal/infra/persistence/gorm/meeting -run "TestPredictionMarketRecapValidationRejectsAShareText|TestTryApplyPredictionMarketRecapBlocksAShareText|TestModeratorRecapPromptForPredictionMarketForbidsExecutableActions|TestApplyMeetingRecapActionsBlocksPaperOrdersForPredictionMarketTeam|TestApplyMeetingRecapActionsFiltersAShareWakePlansForPredictionMarketTeam" -count=1 -v`。
- 2026-06-13：继续补强 Polymarket 搜索输入归一化：支持 `polymarket.com/event/...`、`www.polymarket.com/market/...` 这类无协议裸域名 URL，以及 `us-x-iran-permanent-peace-deal-by#qTlpdC7` 这类带分享 fragment 的纯 slug，统一归一为 Gamma slug 精确查询，非 Polymarket URL 仍保持原样。已通过 `go test ./internal/app/prediction -run "TestNormalizeSearchQueryExtractsPolymarketSlug|TestSearchNormalizesPolymarketURLForProviderAndRepository" -count=1 -v`、`go test ./internal/infra/prediction -run "TestSearchUsesEventSlugEndpointForPolymarketURL|TestSearchFallsBackToMarketSlugEndpoint|TestSearchRequestsFullPublicSearchPayload" -count=1 -v` 和 `go test ./internal/app/prediction ./internal/infra/prediction -count=1`。
- 2026-06-13：收敛预测市场配置元数据与用户可见边界：`/ai/tool-definitions` 中的预测市场 snapshot/orderbook/price history 工具说明同步暴露 outcome/token 映射语义，投研团队列表把预测市场团队显示为不绑定执行账户，设置向导说明明确只有 A 股/混合团队需要模拟盘账户；预测市场角色技能白名单移除带仓位/交易风控语义的通用 `risk-review`，默认风险官改用预测市场结算风险、交叉质询和证据综合技能。已通过 `go test ./internal/app/ai -run TestPredictionToolDefinitionsExplainOutcomeTokenMapping -count=1 -v`、`go test ./internal/app/research -count=1`、`go test ./internal/transport/http -run TestSetupReadinessActionsAreExplicitAndIdempotent -count=1` 和 `npm run build --prefix frontend`。
- 2026-06-13：补强预测市场会议证据工具可解释性：`prediction.market_snapshot`、`prediction.orderbook` 和 `prediction.price_history` 均输出 outcome/token/price 映射，盘口和价格历史会标注当前 token 对应的 outcome、outcome_index 和 outcome_price，避免角色只看到 CLOB token id 后把 Yes/No 方向读反；orderbook/history 工具改为复用可替换的预测市场 provider 工厂，便于工具级回归。已通过 `go test ./internal/infra/persistence/gorm/meeting -run "TestPredictionToolsExposeOutcomeTokenMapping|TestPredictionSearchToolPersistsAndReturnsLocalMarketID|TestPredictionRealtimeTokenIDsEnrichesMarketMetadata" -count=1 -v`。
- 2026-06-13：继续收紧预测市场会议边界：默认预测市场投研团队若来自旧版本，会在默认播种/设置向导动作中修正资产类型、清空模拟盘账户、补齐缺失默认角色、刷新默认预测角色的职责/prompt/工具/技能，同时保留 provider/model/enabled；预测团队自定义/复制角色会被加入“预测市场角色边界”prompt，避免旧 A 股角色说明继续污染结论；`mixed` 团队只有当本次会议实际带有预测市场 ID 时才按 prediction meeting 运行，消息触发建会和会议 role context 都不会回退到 A 股 related symbols 或展示残留 A 股工具。已通过定向测试 `go test ./internal/app/research -count=1`、`go test ./internal/app/messaging -run "TestEnsureMeetingsForPredictionTeamDoesNotFallbackToAShareSymbols|TestEnsureMeetingsForMixedTeamWithPredictionMatchUsesPredictionBoundary|TestFilterMessageClearsRelatedSymbolsForPredictionTeam" -count=1`、`go test ./internal/infra/persistence/gorm/meeting -run "TestMixedTeamMeetingWithPredictionMarketContextUsesPredictionBoundary|TestManagedPromptsForPredictionMarketAvoidAShareTradingFrame|TestApplyMeetingRecapActionsFiltersAShareWakePlansForPredictionMarketTeam|TestPredictionSearchToolPersistsAndReturnsLocalMarketID" -count=1`，以及全量 `go test ./...`、`go vet ./...`、`npm run build --prefix frontend` 和 `git diff --check`（仅 Windows LF/CRLF 提示）。
- 2026-06-13：修复预测市场搜索 URL/slug 召回：`GET /prediction-markets/search` 与会议工具 `prediction.search_markets` 会把 Polymarket event/market URL 归一化为 slug，event slug 使用 Gamma `/events/slug/{slug}`、market slug 使用 `/markets/slug/{slug}` 获取完整 market id、condition id 和 CLOB token ids；普通关键词搜索改为完整 `/public-search` payload，本地兜底搜索覆盖 event 标题/slug、market external id 和 condition id，会议搜索工具会持久化候选并返回本地 market id 以便继续调用 snapshot/orderbook/price history。显式搜索会在 OpenAPI meta 暴露 `normalizedQuery` 和 provider warning，provider 不可用且本地无缓存命中时返回错误；前端预测市场页显示 market 身份、CLOB token 完整性和缓存警告。预测市场会议上下文进一步隔离 A 股：主持人 plan prompt、角色 context、recap context 均使用预测市场语境，默认预测团队主持人可重新搜索市场、反方风险官可查盘口和价格历史，消息触发 prediction team 建会不会从 filter result、消息聚合字段或正文兜底继承 A 股 `related_symbols`，预测市场团队过滤结果即使模型误填也会清空 `related_symbols`，候选匹配只为 `prediction_market`/`mixed` 团队绑定订阅保存，预测市场关注列表读写都要求明确的 `prediction_market`/`mixed` 团队，预测市场角色保存/复制/资产类型切换和前端角色编辑器只允许观察证据工具与预测市场技能，误配置的 `market.*`/`paper.*` 工具会在执行层被拒绝；预测市场会议的 `web.search` 默认使用 global/US Google News 参数，且不会把历史 context 中的 A 股 related symbol 拼入搜索 query；prediction meeting recap 会拒绝 indicator wake plan 和 `code`/`symbol`/`ticker`/`related_symbols` 等 A 股触发字段，只允许观察型 time/event 唤醒，避免被 A 股新闻区域或执行语境带偏。已通过真实 Gamma 探测 `us-x-iran-permanent-peace-deal-by`、`go generate ./internal/transport/http/openapi`、`npm run typegen:api --prefix frontend`、`go test ./...`、`go vet ./...`、`npm run build --prefix frontend` 和 `git diff --check`（仅 Windows LF/CRLF 提示）。
- 2026-06-12：新增预测市场（Polymarket v1）资产域后，系统具备 `prediction_events`、`prediction_markets`、`prediction_market_quotes`、`prediction_market_matches` 和 `prediction_watchlist_items` 表，OpenAPI 暴露 `/prediction-markets/search`、`/prediction-markets/sync`、`/prediction-events/{eventId}`、`/prediction-markets/{marketId}`、`/prediction-matches`、`/prediction-matches/{matchId}/review` 和 `/prediction-watchlist`；`research_teams.assetClass=prediction_market` 允许无模拟盘账户，默认预测市场投研团队和默认预测市场消息过滤器幂等创建，消息过滤后保存新闻到预测市场匹配证据，会议通过 `prediction.*` 工具读取市场快照、盘口、价格历史和匹配证据；前端新增 `/prediction-markets` 页面用于市场发现和匹配治理。已通过 `go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./...`、`go vet ./...`、`golangci-lint run`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 和 `git diff --check`；`git diff --check` 仅输出 Windows LF/CRLF 提示，`npm ci` 仍报告 2 个 moderate 级 npm audit vulnerabilities，`docker compose config` 因本机未安装 Docker CLI 无法执行。已保留预测市场页面截图 `visual-artifacts/prediction-markets-page.png` 作为可视化验证产物。
- 2026-06-12：补充预测市场升级专项验收：`SeedDefaults` 现在会在启动默认播种中幂等创建默认预测市场投研团队和默认预测市场消息过滤器，旧 schema 经 `AutoMigrate` 后会显式归一化 `research_teams.asset_class=a_share` 并创建预测市场新表；新增回归覆盖旧 `research_teams`/`paper_accounts` schema 升级后自动播种预测市场默认项。预测市场消息过滤器和默认角色 prompt 已补强可裁定事件、Gamma 搜索候选、结算/时间窗/流动性/赔率证据、风险反证和禁止交易动作；消息过滤模型调用外层和 JSON 修复提示会保留 `related_prediction_markets`、`match_confidence` 和 `match_reason`；受控会议 prompt 会按 `assetClass=prediction_market` 切换为预测市场语境，不再套用 A 股交易系统约束。已通过 `go test ./internal/infra/persistence/gorm/runtime ./internal/infra/persistence/gorm/connect ./internal/app/research ./internal/app/messaging ./internal/infra/persistence/gorm/meeting -count=1`。
- 2026-06-09：新增旧版本数据库直接升级回归后，`TestAutoMigrateUpgradesPreviousVersionSchemaWithoutRebuild` 会用上一版本关键表形状创建旧 schema 和旧数据，再运行当前 `AutoMigrate`，断言 `auth_sessions`、`audit_events`、`paper_corporate_actions` 表、管理员角色/启停/显示名/最近登录列、消息反馈列均自动补齐，旧管理员、订阅、消息和模拟盘账户数据保留，空过滤状态归一化与空订阅 `collect_from` 清理仍幂等。已通过 `go test ./internal/infra/persistence/gorm/connect -run TestAutoMigrateUpgradesPreviousVersionSchemaWithoutRebuild -count=1 -v`。
- 2026-06-09：完成真实旧版本直升演练：临时 `git worktree add --detach <temp>/old HEAD` 拉出上一公开版本，使用旧版本 `go run ./cmd/tradingcopilot migrate` 创建 SQLite 旧库，再用当前工作树 `go run ./cmd/tradingcopilot migrate` 直接升级同一数据库，随后用 GORM 检查 `admin_users`、`auth_sessions`、`audit_events`、`paper_corporate_actions`、`message_subscriptions`、`ingested_messages` 表以及新增管理员/消息反馈列均存在，输出 `upgrade schema ok`。
- 2026-06-09：本轮产品迭代最终本地验收已完成：`go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./...`、`go vet ./...`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 和 `git diff --check` 均通过；`npm ci` 仍报告 2 个 moderate 级 npm audit vulnerabilities，`git diff --check` 仅输出 Windows LF/CRLF 提示，`docker compose config` 因本机未安装 Docker CLI 无法执行。环境限制下已用一次性 Go YAML 解析器确认 `docker-compose.yml` 包含 `app/postgres/redis`、`docker-compose.integration.yml` 包含 `postgres/redis`，并在本地 `serve` 中验证 `/api/health` 返回 ok。
- 2026-06-09：本地 `serve` 以 `.env.local.example` 启动后，缺少 Telegram MTProto app_id 属于预期未配置路径，消息订阅实时监听器现在记录为 `level=info`、`status=skipped` 和 `reason`，不再写入 error 级近期错误；已通过 `go test ./internal/infra/persistence/gorm/runtime ./internal/infra/logging ./internal/transport/http -count=1`，并用本地日志尾部确认新进程输出为 skipped。
- 2026-06-09：新增 recap action 人工复核 API 后，`POST /meetings/{meetingId}/recap-action-reviews` 会把 `approved`、`needs_evidence`、`rejected` 或 `superseded` 判定写入 `recap_action_review` 会议事件；`approved` 必须带 `confirm=true`，响应和 `trustReport` 会保留来源事件、动作序号、原始 spec、复核人、证据/引用 ID 与 `manual_execution_required` 执行处置，不会自动落地自选、唤醒计划或模拟盘订单。前端会议详情“待复核动作”支持备注、确认有效、补证据和驳回，并展示最近复核结果。已通过 `go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./internal/app/meeting ./internal/transport/http -run "TestReviewRecapAction|TestMeetingRecapActionReview|TestBuildMeetingTrustReport" -count=1`、`go test ./...`、`go vet ./...`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 和 `git diff --check`；`npm ci` 仍报告 2 个 moderate 级 npm audit vulnerabilities，`git diff --check` 仅输出 Windows LF/CRLF 提示，`docker compose config` 因本机未安装 Docker CLI 无法执行。
- 2026-06-09：会议可信度报告新增 recap action 复核队列后，`GET /meetings/{meetingId}` 的 `trustReport` 会聚合 `recap_actions_blocked`/`recap_actions_review_required` 事件中的 `suggested_actions`，暴露 `recapActionReviewStatus`、`recapActionSuggestionCount` 和 `recapActionSuggestions`；前端会议详情“可信度”面板展示待复核动作、原始 spec、处置状态、原因和证据摘要。已通过 `go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./internal/transport/http -run "TestBuildMeetingTrustReport|TestMeetingTrustReviewHTTP" -count=1`、`npm run build --prefix frontend`、`go test ./...`、`go vet ./...` 和 `git diff --check`；`git diff --check` 仅输出 Windows LF/CRLF 提示，`docker compose config` 因本机未安装 Docker CLI 无法执行。
- 2026-06-09：recap action 结构化建议收口后已完成本地全量验收：`go generate ./...`、`go test ./...`、`go vet ./...`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 和 `git diff --check` 均通过；`npm ci` 仍报告 2 个 moderate 级 npm audit vulnerabilities，`git diff --check` 仅输出 Windows LF/CRLF 提示，`docker compose config` 因本机未安装 Docker CLI 无法执行。
- 2026-06-09：新增弱引用 recap action 复核门禁后，带可执行动作的主持人 recap 若只有 `@role` 角色引用、没有工具/来源引用或显式证据事件 ID，会写入 `recap_actions_review_required` 系统事件并跳过自选、唤醒计划和模拟盘订单创建；事件 payload 会保留 `suggested_actions`、`disposition`、`evidence_summary` 和各类动作计数，方便后续人工复核/审批队列消费；带 `market.realtime_quote`、`meeting.context`、`market.symbols` 等来源引用或显式证据 ID 的动作仍可自动执行。已通过 `go test ./internal/infra/persistence/gorm/meeting -run "TestApplyMeetingRecapActions|TestRunMeetingOnceAppliesModeratorRecapActions|TestRunMeetingOnceManagedRunnerRetriesActionRecapWithoutEvidence" -count=1`。
- 2026-06-09：新增首次配置闭环烟测 `TestSetupWizardSmokeE2ECreatesMeetingActions`，覆盖管理员初始化后配置 AI provider、显式执行默认模拟盘/团队/消息过滤器动作、绑定过滤器模型、创建 RSS 订阅、写入行情证券、创建会议，并通过带结构化证据和引用的主持人 recap action 生成自选、唤醒计划和会议来源模拟盘订单；最终再断言 `/setup/readiness` 中 AI、团队、模拟盘、行情和消息源均为 ready。已通过 `go test ./internal/transport/http -run TestSetupWizardSmokeE2ECreatesMeetingActions -count=1`。
- 2026-06-09：会议 recap action 落库前新增二次门禁，`ApplyMeetingRecapActions` 会复用应用层主持人 recap action 校验；缺少结构化事实/推断、缺少引用或唤醒计划语义无效时，会记录 `recap_actions_blocked` 系统事件并跳过自选、唤醒计划和模拟盘订单创建。新增测试覆盖缺证据阻断、无效唤醒计划阻断，以及带证据的自选/唤醒/订单正常落地；已通过 `go test ./internal/app/meeting ./internal/infra/persistence/gorm/meeting -count=1`。
- 2026-06-09：设置向导 readiness 已收敛为只读状态聚合，模拟盘账户/风控和消息过滤器不会在 `GET /setup/readiness` 中被隐式创建；新增 `TestSetupReadinessActionsAreExplicitAndIdempotent` 覆盖管理员初始化、初始缺失状态、`sync-ai-models` 无 provider warning、`test-proxy`、`ensure-paper-defaults`、`ensure-default-team`、`ensure-message-filter` 的重复执行，以及账户、风控、团队、角色、过滤器不重复创建。已通过 `go test ./internal/transport/http -run TestSetupReadinessActionsAreExplicitAndIdempotent -count=1`。
- 2026-06-09：新增 S3/OSS 备份归档外部集成 harness 后，`TC_INTEGRATION_BACKUP_S3=1` 与 `TC_INTEGRATION_BACKUP_OSS=1` 可分别验证真实对象存储上的备份创建、列表、stat、恢复 dry-run 和测试对象删除；测试会在配置 prefix 下追加唯一 `integration/<provider>/<timestamp>` 子前缀，避免触碰既有备份归档。默认 `go test ./...` 不设置环境变量时跳过。已通过 `go test ./internal/integration -count=1`。
- 2026-06-09：新增真实 OSS 备份归档 provider 后，`BACKUP_ARCHIVE_PROVIDER=oss` 且 OSS bucket/region/endpoint/access key/secret key 完整时，HTTP 备份创建、恢复 dry-run、保留策略删除和 Redis worker 周期恢复演练会通过 `BackupArchiveStore` 走阿里云 OSS；`backupMetadataDir` 保留本地恢复演练记录和沙箱目录，S3 与 OSS 均已具备真实对象存储归档能力。已通过 `go test ./internal/infra/backup ./internal/app/ops -count=1`、`go test ./internal/infra/backup ./internal/app/ops ./internal/app/settings ./internal/transport/http ./internal/infra/persistence/gorm/queue -count=1`、`go generate ./...`、`go test ./...`、`go vet ./...`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 和 `git diff --check`；`npm ci` 仍报告 2 个 moderate 级 npm audit vulnerabilities，`git diff --check` 仅报告 Windows LF/CRLF 提示，`docker compose config` 因本机未安装 Docker CLI 无法执行。真实 OSS 端到端上传需要部署环境提供有效 bucket/endpoint/凭据后执行外部集成检查。
- 2026-06-09：新增真实 S3 备份归档 provider 后，`BACKUP_ARCHIVE_PROVIDER=s3` 且 S3 bucket/region/access key/secret key 完整时，HTTP 备份创建、恢复 dry-run、保留策略删除和 Redis worker 周期恢复演练会通过 `BackupArchiveStore` 走 S3 或 S3-compatible endpoint；`backupMetadataDir` 保留本地恢复演练记录和沙箱目录。已通过 `go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./internal/infra/backup ./internal/app/ops ./internal/app/settings ./internal/transport/http ./internal/infra/persistence/gorm/queue ./internal/architecture -count=1`、`npm run build --prefix frontend`、`go mod tidy`、`go test ./...`、`go vet ./...`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 和 `git diff --check`；`git diff --check` 仅报告 Windows LF/CRLF 提示，`docker compose config` 因本机未安装 Docker CLI 无法执行，`npm ci` 仍报告 2 个 moderate 级 npm audit vulnerabilities。真实 S3 端到端上传需要部署环境提供有效 bucket/endpoint/凭据后执行外部集成检查。

- 2026-06-09：新增模拟盘高风险订单复核后，订单资源暴露 `approvalRiskLevel`、`approvalRiskReasons`、`approvalRiskMetrics`、`approvalReviewRequired` 和 `approvalConfirmRequired`，服务端会基于来源证据、账户/风控状态、单笔金额、现金占用、买后仓位和卖出持仓覆盖派生复核结果；`POST /paper/orders/{orderId}/approve` 接受 `paper-order-approvals` 请求体，未带 `confirmHighRisk=true` 的高风险订单会被拒绝提交，前端订单列表展示复核等级并在高风险审批时要求二次确认。已通过 `go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./internal/app/paper ./internal/transport/http ./internal/infra/persistence/gorm/paper -count=1`、`go test ./...`、`go vet ./...`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 和 `git diff --check`；`git diff --check` 仅报告 Windows LF/CRLF 提示，`docker compose config` 因本机未安装 Docker CLI 无法执行，`npm ci` 仍报告 2 个 moderate 级 npm audit vulnerabilities。

- 2026-06-09：新增外部归档诊断配置在设置页维护后，运行时设置 API 会暴露 `BACKUP_ARCHIVE_PROVIDER`、S3/OSS bucket、region、endpoint、prefix 等字段，并对 access key/secret key 只返回 `configured` 或空字符串；前端设置页提供“备份归档”配置区，凭据输入框留空不覆盖现有 `.env` 值，保存时仍会写入 app setting 与当前运行时 `.env`。已通过 `go test ./internal/app/settings ./internal/app/ops ./internal/infra/config ./internal/transport/http -count=1`、`npm run build --prefix frontend`、`go test ./...`、`go vet ./...` 和 `git diff --check`；`docker compose config` 因本机未安装 Docker CLI 无法执行。

- 2026-06-09：新增备份外部归档 provider 就绪度诊断后，`BACKUP_ARCHIVE_PROVIDER=s3|oss` 会在 `/ops/backups`、备份创建结果和恢复 dry-run 结果中暴露 `configuredProvider`、`configuredExternalProvider`、`externalReady`、`missingExternalConfig` 和 `setupHint`；该阶段实际归档仍使用 `local_filesystem`，S3/OSS 只做配置完整性诊断，不伪装远端上传能力。已通过 `go test ./internal/app/ops ./internal/infra/config -count=1`、`go test ./internal/app/ops ./internal/infra/config ./internal/transport/http ./internal/infra/persistence/gorm/queue -count=1`、`go generate ./...`、`npm run typegen:api --prefix frontend`、`npm run build --prefix frontend`、`go test ./...`、`go vet ./...` 和 `git diff --check`；`docker compose config` 因本机未安装 Docker CLI 无法执行。

- 2026-06-09：新增会议可信度报告级证据门禁后，`GET /meetings/{meetingId}` 的 `trustReport.evidenceGate` 会检查 `fact`/`inference` 声明是否绑定结构化证据事件或引用，返回 `pass`、`warning` 或 `blocked`，并暴露缺证明细；主持人最终 recap prompt 也要求输出 `facts`、`assumptions`、`inferences`、`evidence_gaps` 和 `citations`；带 `watchlist_actions`、`wake_plans` 或 `orders` 的 recap 必须提供 `facts`/`inferences` 和 `citations`，否则触发语义重试；前端会议详情展示“证据门禁”和“缺证声明”。已通过 `go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./internal/transport/http -run "TestBuildMeetingTrustReport" -count=1`、`go test ./internal/infra/persistence/gorm/meeting -run "TestRunMeetingOnceManagedRunnerPlansToolRequestsAndRecap|TestRunMeetingOnceManagedRunnerRetriesInvalidWakePlanRecap|TestRunMeetingOnceManagedRunnerRetriesActionRecapWithoutEvidence" -count=1`、`go test ./internal/transport/http -count=1`、`go test ./...`、`go vet ./...` 和 `npm run build --prefix frontend`；`docker compose config` 因本机未安装 Docker CLI 无法执行。

- 2026-06-09：新增来源可信自动治理剧本后，`POST /message-subscriptions/maintenance` 支持 `apply_source_trust_governance`，会基于 `GET /message-feedback/source-trust` 的 `autoAction` 将严重低可信来源暂停采集，将中等风险来源写入治理观察状态，并在订阅 `config.sourceTrustGovernance` 与 `lastCollectError` 中保留原因；前端消息库反馈治理面板新增“应用治理”入口。已通过 `go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./internal/app/messaging -run "Maintenance|SourceTrust" -count=1`、`go test ./internal/app/messaging ./internal/transport/http ./internal/architecture`、`go test ./...`、`go vet ./...`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 和 `git diff --check`；`docker compose config` 因本机未安装 Docker CLI 无法执行，`npm ci` 仍报告 2 个 moderate 级 npm audit vulnerabilities。

- 2026-06-09：新增 Telegram 账号权限巡检后，`POST /message-subscriptions/maintenance` 支持 `audit_telegram_access`，可按订阅 ID/provider/阻塞或预警范围巡检 Telegram 源访问权限；响应新增 `checked`、`passed`、`failed` 统计，失败会写入订阅 `lastCollectError`，成功会清理既有采集错误，前端消息订阅器新增“Telegram 权限巡检”入口。已通过 `go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./internal/app/messaging -run "Maintenance|SubscriptionDiagnostics" -count=1`、`go test ./internal/app/messaging ./internal/transport/http ./internal/architecture`、`go test ./...`、`go vet ./...`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 和 `git diff --check`；`docker compose config` 因本机未安装 Docker CLI 无法执行，`npm ci` 仍报告 2 个 moderate 级 npm audit vulnerabilities。

- 2026-06-09：新增消息订阅维护动作后，`POST /message-subscriptions/maintenance` 支持 `repair_defaults`、`clear_collect_error`、`queue_collect` 和 `rotate_rss_auth`，可按订阅 ID、provider、阻塞/预警状态限定范围；前端消息订阅器新增“修复阻塞源”和“RSS 凭据轮换”入口。已通过 `go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./internal/app/messaging -run "Maintenance|RSS" -count=1`、`go test ./internal/transport/http -run TestMessagingRoutesUseTransportUsecaseAndRequireAdmin -count=1`、`go test ./internal/app/messaging ./internal/transport/http ./internal/architecture`、`go test ./...`、`go vet ./...`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 和 `git diff --check`；`docker compose config` 因本机未安装 Docker CLI 无法执行，`npm ci` 仍报告 2 个 moderate 级 npm audit vulnerabilities。

- 2026-06-09：新增 RSS Basic/Bearer 私有源认证后，`POST/PUT /message-subscriptions` 支持 `rssAuthType`、`rssUsername`、`rssPassword`，RSS 密码/token 走 `SecretKindMessageSubscription` 加密保存且响应只回显 `hasRssPassword`；采集运行时通过 HTTP Authorization header 拉取私有 feed，诊断将 RSS URL 内嵌凭据与专用认证状态分开。已通过 `go test ./internal/app/messaging`、`go test ./internal/infra/messaging` 和 `go test ./internal/transport/http -run TestMessagingRoutesUseTransportUsecaseAndRequireAdmin -count=1`；完整验收仍需 `go generate ./...`、`npm run typegen:api --prefix frontend`、`npm run build --prefix frontend`、`go test ./...`、`go vet ./...`、`git diff --check` 与 `docker compose config`。

- 2026-06-09：新增消息反馈训练集 JSONL 导出归档后，`GET/POST /message-feedback/training-exports` 支持创建/列出导出版本，`GET /message-feedback/training-exports/{exportVersion}` 返回完整 `content`、`contentSha256`、字节数和行数；前端消息库反馈治理面板支持创建导出并下载 `.jsonl`。已通过 `go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./internal/app/messaging`、`go test ./internal/transport/http -run TestMessagingRoutesUseTransportUsecaseAndRequireAdmin -count=1` 和 `npm run build --prefix frontend`；完整验收仍需跑 `go test ./...`、`go vet ./...`、`git diff --check` 与 `docker compose config`。
- 2026-06-09：新增模拟盘基础日线历史回测 API 与前端“历史回测”面板后，`go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./internal/app/paper ./internal/transport/http`、`go test ./...`、`go vet ./...`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 和 `git diff --check` 已通过；`docker compose config` 因本机未安装 Docker CLI 无法执行。`npm ci` 仍报告 2 个 moderate 级 npm audit vulnerabilities。
- 2026-06-09：新增模拟盘部分成交、剩余数量可撤、订单成交进度字段和前端“记录成交”入口后，已通过 `go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./internal/infra/persistence/gorm/paper ./internal/app/paper ./internal/transport/http -count=1`、`go test ./...`、`go vet ./...`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 和 `git diff --check`；`docker compose config` 因本机未安装 Docker CLI 无法执行。`npm ci` 仍报告 2 个 moderate 级 npm audit vulnerabilities。
- 2026-06-09：新增模拟盘自动撮合成交量预算后，自动维护按 `executeAfter, id` 顺序消耗同标的当日实时成交量 10% 参与率预算，预算不足时形成自动部分成交，后续订单保留待执行并写入解释；已通过 `go test ./internal/infra/persistence/gorm/paper -count=1`。
- 2026-06-09：新增模拟盘自动成交价格冲击后，自动维护会基于当日实时成交量参与率叠加不利价格调整，并在 `executionNote` 记录滑点、价格冲击和撮合报告；覆盖测试为 `go test ./internal/infra/persistence/gorm/paper -run TestExecuteDueOrdersCapsAutoFillsByRealtimeVolumeBudget -count=1`。
- 2026-06-09：会议运行器架构边界开始上移后，`internal/app/meeting` 新增 run 启动策略、终态判断、启动事件 payload 和 legacy 完成默认文案，`internal/infra/persistence/gorm/meeting` 复用应用层策略但仍保留 DB 持久化与 managed flow；已通过 `go test ./internal/app/meeting ./internal/infra/persistence/gorm/meeting ./internal/architecture -count=1`、`go test ./...` 和 `go vet ./...`。
- 2026-06-09：会议运行器继续上移角色输出归一化和主持人 fallback 计划策略后，`internal/app/meeting` 负责清洗结构化事实/假设/推断/证据缺口、问题/提及/引用和置信度，GORM runner 只负责调用模型、执行工具和落库事件；覆盖测试为 `go test ./internal/app/meeting ./internal/infra/persistence/gorm/meeting -count=1`。
- 2026-06-09：主持人 recap 可执行动作证据校验和唤醒计划语义校验已上移到 `internal/app/meeting.ValidateModeratorRecapActions`，GORM runner 只代理调用应用层策略；覆盖测试为 `go test ./internal/app/meeting -count=1` 与 `go test ./internal/infra/persistence/gorm/meeting -run "TestRunMeetingOnceManagedRunnerPlansToolRequestsAndRecap|TestRunMeetingOnceManagedRunnerRetriesInvalidWakePlanRecap|TestRunMeetingOnceManagedRunnerRetriesActionRecapWithoutEvidence" -count=1`。

- 2026-06-09：新增 OTLP exporter、采样配置和队列 trace envelope 后，`go test ./internal/infra/tracing -count=1`、`go test ./internal/infra/queue -count=1`、`go test ./internal/app/settings -count=1`、`go test ./internal/app/meeting -count=1` 与 `go test ./internal/infra/persistence/gorm/queue -count=1` 覆盖配置解析、运行时设置校验、legacy payload 兼容和 worker 消费时 trace ID 延续；完整验收仍需跑 `go test ./...`、`go vet ./...`、`npm run build --prefix frontend` 与 `docker compose config`。

当前本地验证：
- 2026-06-09：新增消息人工反馈批处理后，`POST /ingested-messages/feedback/batch` 支持对最多 500 条摄取消息批量写入 `helpful`、`noise`、`misclassified` 或 `neutral` 反馈，响应返回请求数量、更新数量、更新 ID 和缺失 ID；前端消息库桌面表格支持多选后批量反馈。已通过 `go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./internal/app/messaging -run TestFeedbackMessagesBatchUpdatesFoundAndReportsMissing -count=1`、`go test ./internal/transport/http -run TestMessagingRoutesUseTransportUsecaseAndRequireAdmin -count=1`、`go test ./internal/app/messaging ./internal/transport/http` 和 `npm run build --prefix frontend`。
- 2026-06-09：新增消息订阅来源诊断后，`GET /message-subscriptions/diagnostics` 返回 `message-subscription-diagnostics` collection，按订阅源暴露 `sourceKind`、`proxyRoute`、`status`、`checks` 和 `recommendedActions`；前端消息订阅器新增“来源诊断”面板。已通过 `go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./internal/app/messaging ./internal/transport/http`、`go test ./...`、`go vet ./...`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 和 `git diff --check`；`docker compose config` 因本机未安装 Docker CLI 无法执行，`npm ci` 仍报告 2 个 moderate 级 npm audit vulnerabilities。
- 2026-06-09：新增来源可信自动降权策略后，低可信订阅源会把过滤器输出的 `meeting` 降为 `observe`，极低可信或严重负反馈来源会把 `observe` 降为 `ignore`，降权原因写入 `filterReason`，`GET /message-feedback/source-trust` 暴露 `autoAction` 和 `autoActionReason`，前端消息库展示自动策略列；已通过 `go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./internal/app/messaging -run "TestFilterMessageAppliesSourceTrustDownrank|TestFeedbackTrainingSamplesAndSourceTrustReport|TestFilterMessageOwnsPersistence" -count=1`、`go test ./internal/transport/http -run TestMessagingRoutesUseTransportUsecaseAndRequireAdmin -count=1` 与 `npm run build --prefix frontend`。
- 2026-06-09：新增消息反馈离线评估与训练集版本快照后，`GET /message-feedback/evaluation` 返回反馈样本质量指标，`GET/POST /message-feedback/training-snapshots` 支持创建/列出带指纹和样本 manifest 的训练快照，前端消息库展示评估指标与快照列表；已通过 `go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./internal/app/messaging ./internal/transport/http` 和 `npm run build --prefix frontend`，完整验收仍需跑 `go test ./...`、`go vet ./...`、`npm ci --prefix frontend`、`git diff --check` 与 `docker compose config`。
- 2026-06-09：新增消息反馈训练样本和来源可信治理接口后，`go test ./internal/app/messaging -run TestFeedbackTrainingSamplesAndSourceTrustReport -count=1`、`go test ./internal/infra/persistence/gorm/repo -run TestMessagingRepositoryCRUDAndFilters -count=1` 和 `go test ./internal/transport/http -run TestMessagingRoutesUseTransportUsecaseAndRequireAdmin -count=1` 应覆盖样本导出、订阅预加载、可信评分聚合、重算报告和前端消息库治理入口；完整验收仍需跑 `go test ./...`、`go vet ./...`、`go generate ./...`、`npm run typegen:api --prefix frontend`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 与 `git diff --check`。
- 2026-06-09：细化 `Dashboard`、`OpsBackups`、`OpsBackupRun` 和 `PaperPerformancePoint` OpenAPI schema 后，前端 `api.ts` 对应类型改为从生成 schema 派生，仅保留金额/比例展示覆盖；验证重点为 `go generate ./...`、`npm run typegen:api --prefix frontend` 和 `npm run build --prefix frontend`。
- 2026-06-09：新增结论句人工复核工作流后，`POST /meetings/{meetingId}/trust-reviews` 写入 `trust_review` 会议系统事件，`GET /meetings/{meetingId}` 的 `trustReport.sentenceReviews` 聚合复核判定、引用 ID、证据事件 ID、复核人和备注；`go test ./internal/app/meeting -count=1`、`go test ./internal/transport/http -count=1`、`go test ./internal/architecture -count=1`、`go test ./...`、`npm run typegen:api --prefix frontend` 和 `npm run build --prefix frontend` 已通过。
- 2026-06-09：新增 HTTP OpenTelemetry trace context 提取和请求 span 后，`go test ./internal/transport/http -run TestTraceRequestsExtractsTraceContext -count=1 -v` 覆盖 `traceparent` 传播到请求上下文；完整验收仍需跑 `go test ./...`、`go vet ./...` 和 `git diff --check`。
- 2026-06-09：新增行情后台任务 `POST /market/tasks` 与 Redis/asynq `market_run_task` 后，`go test ./internal/app/market -count=1`、`go test ./internal/infra/queue -count=1`、`go test ./internal/infra/persistence/gorm/queue -count=1` 和 `go test ./internal/transport/http -count=1` 覆盖任务入队、worker 分发、provider 错误返回队列重试以及本地未配置队列时的 HTTP 409 路径；完整验收仍需跑 `go test ./...`、`go vet ./...`、`go generate ./...`、`npm run typegen:api --prefix frontend`、`npm run build --prefix frontend` 与 `git diff --check`。

- 2026-06-09：新增 AI provider/model 价格覆盖治理后，`GET /ops/provider-health` 暴露启用模型价格覆盖率、缺价格模型样本，`GET /ops/metrics` 暴露 `tradingcopilot_ai_cost_rate_*` 指标；相关覆盖应包含 `go test ./internal/infra/persistence/gorm/dashboard -run TestAIUsageDiagnosticsSummarizesMeetingEventUsage -count=1` 与 `go test ./internal/transport/http -count=1`。
- 2026-06-09：新增 AI provider latency 观测后，模型调用事件写入 `latency_ms`，`GET /ops/provider-health` 和 `GET /ops/metrics` 暴露 24h/累计延迟观测数、平均延迟和 P95；相关覆盖应包含 `go test ./internal/infra/persistence/gorm/dashboard -run TestAIUsageDiagnosticsSummarizesMeetingEventUsage -count=1` 与 `go test ./internal/transport/http -count=1`。
- 2026-06-09：新增周期性备份恢复演练调度后，`go test ./internal/app/ops -count=1`、`go test ./internal/infra/queue -count=1` 和 `go test ./internal/infra/persistence/gorm/queue -count=1` 应覆盖备份服务、队列任务分发和 worker 最新备份 dry-run；完整验收仍需跑 `go test ./...`、`go vet ./...`、`go generate ./...`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 与 `git diff --check`。
- 2026-06-09：新增备份归档 provider 边界后，`GET /ops/backups`、`POST /ops/backups` 和 `POST /ops/backups/{backupName}/restore-dry-run` 暴露 `archiveProvider`/`archiveProviderDetail`，当前内置 `local_filesystem` provider；创建、读取、dry-run 和保留删除都走 `BackupArchiveStore`，为后续 S3/OSS provider 铺路。覆盖测试为 `go test ./internal/app/ops -count=1`，前端契约需跑 `go generate ./...`、`npm run typegen:api --prefix frontend` 和 `npm run build --prefix frontend`。
- 2026-05-11：`go test ./...` 已在此工作区通过。
- 2026-06-08：`go test ./...`、`go vet ./...` 和 `npm run build --prefix frontend` 已在本工作区通过，覆盖设置向导、唤醒计划编辑、团队安全、审计、运维状态、模拟盘订单审批和会议可信度新增能力。
- 2026-06-08：新增模拟盘 T+1/涨跌停/停牌/滑点/过期单、Prometheus 指标、AI token 用量聚合、备份沙箱恢复演练与保留策略自动执行、provider 数据可靠性诊断和主动登出会话撤销后，已通过相关包测试与前端构建。
- 2026-06-08：新增模拟盘组合归因和风险预警后，`go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./...`、`go vet ./...`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 和 `git diff --check` 已通过。本机未安装 Docker，`docker compose config` 无法执行。
- 2026-06-08：新增 AI 成本价格表、每日成本预算、provider health 成本诊断和 Prometheus 成本指标后，`go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./...`、`go vet ./...`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 和 `git diff --check` 已通过。本机未安装 Docker，`docker compose config` 无法执行。
- 2026-06-08：新增队列积压风险、失败任务样本、`POST /ops/jobs/retry-failed` 和近期错误日志摘要后，相关 HTTP 测试覆盖本地模式禁用路径，Redis/asynq 真实重试仍按外部集成检查验证。
- 2026-06-08：新增模拟盘公司行为和复盘时间线后，`go generate ./...`、`npm run typegen:api --prefix frontend`、`go test ./...`、`go vet ./...`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 和 `git diff --check` 已通过。本机未安装 Docker，`docker compose config` 无法执行。
- 2026-06-09：新增 HTTP RBAC 角色边界后，`go test ./internal/transport/http -run TestRolePermissionsConstrainViewerAndOperator -count=1 -v`、`go test ./internal/transport/http -count=1`、`go test ./internal/app/auth ./internal/transport/http -count=1`、`go test ./...`、`go vet ./...`、`go generate ./...`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 和 `git diff --check` 已通过，覆盖 viewer 只读/自助登出、operator 拒绝安全与配置类写入、operator 允许模拟盘和队列重试业务动作。本机未安装 Docker，`docker compose config` 无法执行。
- 2026-06-09：新增会议可信度提示词哈希快照、声明到证据/引用绑定和前端绑定展示后，`go test ./internal/infra/persistence/gorm/meeting ./internal/transport/http -count=1`、`go test ./...`、`go vet ./...`、`go generate ./...`、`npm run typegen:api --prefix frontend`、`npm ci --prefix frontend`、`npm run build --prefix frontend` 和 `git diff --check` 已通过；Vite dev server `/meetings/1` SPA 路径返回 200。本机未安装 Docker，`docker compose config` 无法执行。
- 2026-06-09：新增逐句结论 diff 后，`go test ./internal/transport/http -run TestBuildMeetingTrustReportKeepsStructuredClaimsAndConclusionDiffs -count=1 -v`、`go test ./internal/transport/http -count=1`、`go test ./...`、`go vet ./...`、`npm run build --prefix frontend` 和 `git diff --check` 已通过，覆盖新增/移除/保留句子的计数和样例；前端会议详情结论变更摘要显示逐句变化数量。本机未安装 Docker，`docker compose config` 无法执行。
- 2026-06-09：前端核心 API 模型从手写接口迁移到 OpenAPI 派生的扁平资源类型后，`npm run build --prefix frontend` 已通过，覆盖设置向导、团队安全、运维队列/日志、消息、研究团队、唤醒计划、模拟盘主要资源、Dashboard、OpsBackups 与 PaperPerformance 曲线点类型收敛。
- 2026-06-09：消息过滤 worker 失败改为“持久化 `failed` 状态 + 向队列返回可重试错误”后，`go test ./internal/app/messaging -run TestProcessFilterTaskReturnsRetryableErrorAfterPersistingFilterFailure -count=1 -v`、`go test ./internal/app/messaging -count=1`、`go test ./internal/infra/queue -count=1` 和 `go test ./internal/infra/persistence/gorm/queue -count=1` 已通过，覆盖 provider/config 失败进入 Redis/asynq retry/archive 死信治理路径前的应用层状态落库。

外部集成验证方式记录在 `doc/integration-harness.md`。带日期的外部结果只是当时快照，不保证相同服务当前仍然可访问。

## 必需本地检查

| 范围 | 必需覆盖 | 主要检查 |
| --- | --- | --- |
| 架构边界 | `domain`/`app`/`transport` 导入边界、GORM 持久化边界、与提供方无关的消息运行时位置、OpenAPI 包装路由、仅 JSON:API 请求解码、camelCase API 参数、模拟交易风险配置绑定契约、日志契约、被忽略的本地路径，以及已移除的旧包 | `go test ./internal/architecture` |
| 完整 Go 套件 | 单元测试、跳过集成测试后的套件、`transport`、`persistence`、`runtime` 和 `architecture` 包 | `go test ./...` |
| API 契约 | 生成的 OpenAPI 服务端接口、路由挂载、JSON:API 响应和错误形状、受保护路由未授权行为、公开路由冒烟覆盖，以及消息领域路由重点覆盖 | `go test ./internal/transport/http ./internal/transport/http/jsonapi` |
| 数据库契约 | 34 张表表面、GORM 模型表名、Timescale 设置顺序、JSON 默认载荷、关键唯一约束，以及关键外键关系 | `go test ./internal/infra/persistence/gorm/connect ./internal/infra/persistence/gorm/repo` |
| 前端契约 | 从 OpenAPI 生成的 TypeScript API 类型和 Vue 生产构建 | `npm run typegen:api --prefix frontend`; `npm run build --prefix frontend` |
| 静态部署配置 | Compose 语法和服务图；验证环境需要安装 Docker | `docker compose config` |

## 领域覆盖

| 领域 | 必需场景 | 主要检查 |
| --- | --- | --- |
| 认证和安全 | 管理员初始化/登录/主动登出、JWT 验证、密码哈希、会话记录/撤销、自助登出后的 token 拒绝、前端 sessionStorage 存储与旧 localStorage token 清理、用户角色、HTTP RBAC、审计事件、兼容 Fernet 的密钥加密，以及受保护路由拒绝未授权/越权请求 | `go test ./internal/app/auth ./internal/infra/security ./internal/transport/http`; `npm run build --prefix frontend` |
| 设置 | 运行时环境变量列表、应用设置覆盖、可编辑 `.env` 回写、AI 成本预算和价格表校验、密钥定义、加密密钥写入/列表，以及代理设置和测试诊断 | `go test ./internal/app/settings ./internal/infra/config ./internal/transport/http` |
| 首次上手 | 设置向导状态聚合、幂等初始化动作、AI/团队/模拟盘/行情/消息/代理/日志/队列完成度，以及前端动作后刷新 | `go test ./internal/transport/http`; `npm run build --prefix frontend` |
| 日志 | 结构化 JSONL 日志、必需的 `event`/`group`/`method`/`status`/可空 `durationMs` 字段、`not_applicable` 填充行为、按大小/时间轮转设置、日志查询 API、HTTP/GORM/后台诊断、脱敏、日志页 URL query 筛选初始化，以及前端日志查看器契约 | `go test ./internal/infra/logging ./internal/transport/http ./internal/architecture`; `npm run build --prefix frontend` |
| AI | 提供方增删改查、加密 API key 使用、OpenAI 兼容客户端解析、SSE/非 SSE 用量解析、重试分类、模型同步、角色增删改查、默认角色能力应用，以及 tokenizer fallback 计数 | `go test ./internal/infra/ai/client ./internal/app/ai ./internal/infra/persistence/gorm/meeting` |
| 研究团队 | 团队增删改查、一个模拟交易账户只能绑定一个团队、启动时默认团队种子、每团队会议角色、默认角色破坏性重置、消息订阅团队绑定，以及按团队限定的会议/自选列表/唤醒计划 | `go test ./internal/app/research ./internal/transport/http ./internal/architecture` |
| 行情 | A 股代码规范化、`adata-go` 映射、Tushare 日线解析/刷新、五日兼容路径、实时缓存 TTL、自选列表报价刷新、陈旧缓存兜底、已废弃提供方错误、标的/自选列表增删改查、后台任务入队、`market_run_task` worker 分发、provider 失败返回队列重试，以及工具查询名称 | `go test ./internal/app/market ./internal/infra/queue ./internal/infra/persistence/gorm/queue ./internal/infra/persistence/gorm/marketdata` |
| 预测市场 | Polymarket Gamma 搜索和活跃市场同步、CLOB 盘口/价格历史工具、独立预测市场表、新闻到市场匹配阈值和证据保存、人工确认/驳回、默认预测团队/过滤器、`assetClass=prediction_market` 无模拟盘账户、消息触发会议时写入预测市场引用、预测市场 recap 不创建模拟盘订单、Market 代理复用和 provider health 诊断 | `go test ./internal/app/prediction ./internal/app/messaging ./internal/app/research ./internal/infra/persistence/gorm/repo ./internal/infra/persistence/gorm/meeting ./internal/transport/http`; `npm run build --prefix frontend`; 浏览器验证 `/prediction-markets` |
| Telegram | App/bot 配置、频道/消息增删改查、引用规范化、AI 过滤、MTProto 登录/session/代理处理、公开/私有采集和回填行为、Bot 发送/轮询命令，以及会议引用创建/删除 | `go test ./internal/app/telegram ./internal/infra/persistence/gorm/telegram` |
| 消息 | 与提供方无关的 Telegram 和 RSS/Atom 订阅、可复用消息过滤器及每订阅绑定/默认选择、订阅源诊断、字符串来源消息身份、通过 gofeed 解析 RSS/Atom、每来源轮询状态、异步摄取消息过滤状态和队列处理、单条/批量人工反馈、反馈训练样本导出与来源可信评分治理、平台适配器增删改查/密钥处理、监听器标题/消息摄取、运行时适配器与 GORM 持久化分离，以及应用层拥有过滤/会议写入的事务边界 | `go test ./internal/app/messaging ./internal/infra/messaging ./internal/infra/queue ./internal/infra/persistence/gorm/repo ./internal/infra/persistence/gorm/runtime ./internal/transport/http` |
| 会议 | 增删改查、列表过滤、详情可信度报告、结构化事实/假设/推断/证据缺口、声明到证据/引用绑定、证据门禁、提示词哈希快照、模型配置快照、逐句结论变更摘要、结论句人工复核、引用、SSE 流、取消/删除/重启语义、回顾、本地取消、托管运行器的规划/工具/JSON 修复/回顾路径、提示词和错误 golden 片段、token 预算限制、会议运行耗时/事件计数诊断，以及 Telegram 完成通知 | `go test ./internal/app/meeting ./internal/infra/persistence/gorm/meeting ./internal/infra/persistence/gorm/dashboard ./internal/transport/http` |
| 唤醒计划 | 增删改查和状态转换、到期计划处理、来源会议上下文、复合指标语法、Telegram 事件匹配、条件未满足时重新调度、旧事件抑制、worker 分发回调，以及可取消循环 | `go test ./internal/app/wake ./internal/infra/persistence/gorm/meeting ./internal/transport/http` |
| 模拟交易 | 风险配置默认值、账户绑定/解绑、未配置账户拒绝、会议订单审批/拒绝、板块校验、ETF/LOF 处理、仓位规模、费用/成交/盈亏、部分成交、剩余数量可撤、T+1 可卖数量、涨跌停、停牌近似、自动滑点、成交量参与率价格冲击、过期单、订单生命周期、定时维护、绩效回撤/胜率边界、组合归因、风险预警、现金分红/送股/拆股公司行为、订单/成交/公司行为复盘时间线，以及 API 增删改查/错误路径 | `go test ./internal/app/paper ./internal/infra/persistence/gorm/paper ./internal/transport/http` |
| 仪表盘和运行时 | 数据库/Redis/提供方诊断、AI model call/token/cost 用量聚合、成本预算状态、缺价格调用统计、行情新鲜度、消息过滤失败/采集错误/去重策略、Redis/asynq 队列快照、队列积压风险、失败任务样本、失败任务筛选、批量/单任务重试动作、近期错误日志摘要、日志跳转联动、Prometheus 指标、HTTP OpenTelemetry trace context、worker/scheduler/paper/Telegram 心跳状态、业务指标、运维状态 API、备份状态报告、沙箱恢复演练、最近演练记录、备份保留策略自动执行、逾期唤醒警报、新闻过滤就绪状态、本地后台循环、worker mux 处理，以及陈旧会议恢复 | `go test ./internal/infra/persistence/gorm/dashboard ./internal/infra/persistence/gorm/runtime ./internal/infra/persistence/gorm/queue ./internal/transport/http`; `npm run build --prefix frontend` |

## 日志语义

- 预期的空操作、保护分支或重复抑制路径（Expected no-op, guard, or duplicate-suppression paths）必须使用 `level=info` 和 `status=skipped`；`level=error` 只用于需要兜底、重试、操作员处理或调用方可见错误处理的失败（reserved for failures）。

## 外部集成检查

这些检查是可选的，因为它们需要外部服务、凭据或部署网络路径。

| 检查 | 何时需要 | 命令 |
| --- | --- | --- |
| Redis/asynq | 验证队列连通性、worker 消费或 scheduler 入队语义 | `TC_INTEGRATION_REDIS=1 go test ./internal/integration -run TestRedisAsynqIntegrationHarness -v -count=1` |
| PostgreSQL/TimescaleDB | 验证生产 schema 初始化或 Timescale hypertable | `TC_INTEGRATION_POSTGRES=1 TC_REQUIRE_TIMESCALE=1 go test ./internal/integration -run TestPostgresTimescaleSchemaInitHarness -v -count=1` |
| 行情提供方 | 验证部署网络访问实时行情提供方的出口能力 | `TC_INTEGRATION_MARKET=1 go test ./internal/integration -run TestMarketProviderIntegrationHarness -v -count=1` |
| AI 提供方 | 验证 OpenAI 兼容提供方/模型/API key 组合 | `TC_INTEGRATION_AI=1 go test ./internal/integration -run TestAIProviderIntegrationHarness -v -count=1` |
| S3 备份归档 | 验证 S3 或 S3-compatible bucket 上的备份创建、列表、读取、恢复 dry-run 和删除 | `TC_INTEGRATION_BACKUP_S3=1 go test ./internal/integration -run TestS3BackupArchiveIntegrationHarness -v -count=1` |
| OSS 备份归档 | 验证阿里云 OSS bucket 上的备份创建、列表、读取、恢复 dry-run 和删除 | `TC_INTEGRATION_BACKUP_OSS=1 go test ./internal/integration -run TestOSSBackupArchiveIntegrationHarness -v -count=1` |
| Telegram MTProto | 验证 app 凭据、已存 session、频道访问和最新消息拉取 | `TC_INTEGRATION_TELEGRAM=1 go test ./internal/integration -run TestTelegramMTProtoIntegrationHarness -v -count=1` |
| Polymarket 公共 API | 验证部署网络和 Market 代理可访问 Gamma/CLOB；默认可通过预测市场同步、搜索和会议工具拉取 REST 数据 | `POST /settings/proxy/test`；`POST /prediction-markets/sync`；会议工具 `prediction.orderbook` / `prediction.price_history` |

## 维护规则

- 每次行为变更影响 API 形状、持久化形状、后台处理或外部集成边界时，都要新增或更新测试。
- Redis/asynq 周期任务必须按 task type 合并，并使用稳定的 `periodic:<task_type>` ID。不要创建按窗口变化的周期任务 ID，也不要附加可能把 scheduler 停机转换为大量失败积压任务的过期或截止时间选项。
- 消息摄取和回填路径不得同步调用 AI 过滤；它们必须先持久化消息和过滤状态，再把消息过滤工作加入队列。
- 保持 `api/openapi.yaml`、生成的 Go 服务端类型、生成的前端类型和 `doc/api-contract.md` 对齐。
- 保持 `doc/database-contract.md` 与 GORM 模型表名、Timescale 设置 SQL 和 schema 初始化测试对齐。
- 在自己的环境中依赖提供方、网络或凭据路径前，重新运行相关集成检查。
